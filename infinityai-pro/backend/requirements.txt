fastapi==0.104.1
uvicorn==0.24.0
requests
python-dotenv
websockets
dhanhq
psutil
pydantic
python-multipart
pandas==2.2.0
numpy==1.26.3
aiohttp
python-dateutil
scikit-learn

# Basic AI Dependencies (lightweight)
# torch and heavy packages will be installed at runtime
transformers>=4.30.0
httpx>=0.25.0
pillow>=10.0.0
# chromadb>=0.4.0  # Commented out temporarily for space
# openai-whisper>=20240930  # Commented out temporarily for space
# torch>=2.0.0  # Will install at runtime
# sentence-transformers>=2.2.0  # Will install at runtime

# Multi-Cloud AI Dependencies
boto3>=1.28.0  # AWS SDK
azure-storage-blob>=12.17.0  # Azure Blob Storage
openai>=1.0.0  # OpenAI SDK (supports Azure OpenAI)
azure-cognitiveservices-speech>=1.32.1  # Azure Speech
# azure-ai-vision>=0.15.0  # Commented out - package not available
# azure-ai-textanalytics>=5.3.0  # Commented out - may have compatibility issues
# azure-ai-ml>=1.11.0  # Commented out - may have compatibility issues

# Hugging Face for Local AI Models
transformers>=4.30.0  # Hugging Face transformers
datasets>=2.14.0  # Hugging Face datasets
accelerate>=0.20.0  # Hugging Face accelerate
torch>=2.0.0  # PyTorch for local models
sentence-transformers>=2.2.0  # Sentence transformers

# ML and AI Dependencies
lightgbm>=4.0.0  # LightGBM for signal generation
prophet>=1.1.0  # Facebook Prophet for time series
scikit-learn>=1.3.0  # Machine learning utilities
xgboost>=1.7.0  # XGBoost alternative

# Additional utilities
tenacity>=8.2.0  # Retry logic for API calls
structlog>=23.1.0  # Structured logging
asyncio-mqtt>=0.13.0  # MQTT for real-time data (if needed)
