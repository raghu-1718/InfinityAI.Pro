FROM chromadb/chroma:latest

# Set environment variables
ENV CHROMA_HOST=0.0.0.0
ENV CHROMA_PORT=8000

# Expose the port
EXPOSE 8000

# Use the default command from the Chroma image
# The latest Chroma image should start automatically