# Provider Integration Environment Template (Phase 7)
# Copy entries to .env and load secure values from GCP Secret Manager.

GOOGLE_CLOUD_PROJECT=galvanic-pulsar-482815-h0
PUBSUB_TOPIC_MARKET_DATA_RAW=market-data.raw
PUBSUB_TOPIC_NEWS_RAW=news.raw

# Default provider selection (e.g., "polygon", "alpha_vantage", "newsapi")
PROVIDER_DEFAULT=none

# Example placeholders (replace with Secret Manager references)
PROVIDER_X_API_KEY=from-secret-manager
PROVIDER_X_ENDPOINT=https://api.example.com
