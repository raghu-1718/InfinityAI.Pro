# Engine Core (API Gateway & Orchestrator) - Production Environment
# Uses Cloud Run environment variables and Secret Manager

PORT=8080
DEBUG=false
LOG_LEVEL=INFO

# Firestore
FIRESTORE_PROJECT_ID=after-yesterday-473512-k3

# Engine URLs (Cloud Run Internal)
ENGINE_ANALYTICS_URL=https://infinityai-engine-analytics-XXXX.a.run.app
ENGINE_EXECUTION_URL=https://infinityai-engine-execution-XXXX.a.run.app

# JWT / Auth (from Secret Manager)
JWT_SECRET_KEY=projects/after-yesterday-473512-k3/secrets/jwt-secret-key/versions/latest
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS
ALLOWED_ORIGINS=https://infinityai.pro,https://www.infinityai.pro

# Environment
ENVIRONMENT=production
