{
  "name": "infinityai-frontend-verifier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test --config=playwright.config.ts",
    "install-browsers": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
