name: Engine B CI

on:
  push:
    branches: [ main ]
    paths:
      - 'infinityai-pro/backend/engines/engine-b/**'
      - '.github/workflows/ci-engine-b.yml'
  pull_request:
    paths:
      - 'infinityai-pro/backend/engines/engine-b/**'
  workflow_dispatch:

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
            python-version: '3.11'
      - name: Install light deps
        working-directory: infinityai-pro/backend/engines/engine-b
        run: |
          if [ -f requirements.txt ]; then grep -E '^(fastapi|uvicorn|pydantic|numpy|pandas|scikit-learn|httpx)' requirements.txt > req.txt || true; fi
          pip install --no-cache-dir -r req.txt || true
          python -c "import sys,types;[sys.modules.setdefault(m,types.ModuleType(m)) for m in ['torch','transformers','asyncpg','redis','aiokafka']];print('Stub modules prepared')"
      - name: Import smoke
        working-directory: infinityai-pro/backend/engines/engine-b
        run: python -c "import sys;\ntry:\n import main; print('Engine B import OK (light mode)')\nexcept Exception as e:\n print('Engine B import failed:', e); sys.exit(1)"
