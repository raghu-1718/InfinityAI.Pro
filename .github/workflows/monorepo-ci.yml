name: Monorepo CI - multi-cloud minimal

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Monorepo CI placeholder - workflows split across files."
