name: DISABLED — Duplicate Deploy (use deploy_production.yml)

on:
  # Disabled to prevent duplicate deployments; kept for historical reference only
  workflow_dispatch:

jobs:
  no-op:
    name: Disabled workflow
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - run: echo "This workflow is disabled. Use .github/workflows/deploy_production.yml instead."
