# Engine A (entrypoint)

This folder serves as the top-level entrypoint for Engine A in the v4.5 layout.

The actual, advanced implementation lives at `../engines/engine-a`.

Use this entrypoint for Docker builds and local runs to keep a clean top-level layout.
