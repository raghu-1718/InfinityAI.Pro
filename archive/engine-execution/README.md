# Archived Engine Execution

This directory previously contained an alternative implementation of Engine C (trade execution and coupon auth).

- The canonical, production-ready backend is now `backend/engine-c`.
- All deployments, CI/CD, and integrations should use `engine-c` only.
- This directory is retained for historical reference and will be deleted in a future cleanup.
